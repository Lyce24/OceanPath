name: tcga_nsclc

# ── Paths ─────────────────────────────────────────────────────────────────────
# Point feature_h5_dir to wherever the HF-downloaded H5 files live.
feature_h5_dir: /mnt/d/YC.Liu/UNI2_h_features

mmap_dir: ./mmap/tcga_nsclc

slide_dir: null
csv_path: ${platform.project_root}/manifests/SSL/tcga_nsclc_labels.csv

# ── Schema ────────────────────────────────────────────────────────────────────
label_columns:
  - label
filename_column: slide_path
patient_id_column: patient_id

wsi_extensions: []
custom_list_of_wsis: null

# ── Feature properties ────────────────────────────────────────────────────────
# UNI2-h produces 1536-dim features
feature_dim: 1536
