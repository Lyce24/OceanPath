defaults:
  - base
  - _self_

ssl_method: dino

ssl:
  proj_hidden_dim: 2048
  proj_out_dim: 256
  proj_num_layers: 3
  n_prototypes: 4096
  bottleneck_dim: 256
  teacher_temp: 0.04
  student_temp: 0.1
  center_momentum: 0.9
  ema_momentum: 0.996
  ema_final_momentum: 1.0
