scheme: custom_kfold
name: gej_kfold5_seed42

seed: 42
n_folds: 5

label_column: ${data.label_columns.0}
group_column: ${data.patient_id_column}
site_column: null

# ── Custom test set rule ──────────────────────────────────────────────────────
# All rows matching this pandas query are fixed as test.
# The remaining rows are split into train/val via k-fold.
test_filter: null
val_filter: null

# ── Not used ──────────────────────────────────────────────────────────────────
train_ratio: null
val_ratio: null
test_ratio: 0.2
n_repeats: null
