# ── Splits: standard holdout (80/10/10) ───────────────────────────────────────
#
# Produces: slide_id, [patient_id], filename, split (train/val/test)

scheme: holdout
name: holdout_80_10_10_seed42

seed: 42

label_column: ${data.label_columns.0}
group_column: ${data.patient_id_column}
site_column: null

# ── Ratios ────────────────────────────────────────────────────────────────────
train_ratio: 0.8
val_ratio: 0.1
test_ratio: 0.1

# ── Not used for holdout ──────────────────────────────────────────────────────
n_folds: null
n_repeats: null
test_filter: null
val_filter: null
