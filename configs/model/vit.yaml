name: vit
arch: vit

# ── Architecture ──────────────────────────────────────────────────────────────
embed_dim: 768
depth: 4
num_heads: 8
mlp_ratio: 4.0
qkv_bias: true
qk_norm: false
init_values: null
drop_rate: 0.0
attn_drop_rate: 0.0
drop_path_rate: 0.1
coord_scale: 512.0              # ← set to your patch_size_lv0
gradient_checkpointing: true
