# Pre-commit hooks for OceanPath
# Install: pre-commit install
# Run all: pre-commit run --all-files
# Update:  pre-commit autoupdate

repos:
  # ── Standard file hygiene ────────────────────────────────────────────
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
        args: [--markdown-linebreak-ext=md]
      - id: end-of-file-fixer
      - id: check-yaml
        args: [--allow-multiple-documents]
      - id: check-toml
      - id: check-added-large-files
        args: [--maxkb=500]
      - id: check-merge-conflict
      - id: debug-statements           # no pdb left in code
      - id: check-ast                   # valid Python syntax
      - id: mixed-line-ending

  # ── YAML lint (catches Hydra config errors) ──────────────────────────
  - repo: https://github.com/adrienverge/yamllint
    rev: v1.38.0
    hooks:
      - id: yamllint
        args: [-d, "{extends: relaxed, rules: {line-length: {max: 120}}}"]
        files: \.(yaml|yml)$
        exclude: uv\.lock

  # ── Mypy (optional — run manually or in CI) ─────────────────────────
  # Uncomment when ready for strict type checking:
  # - repo: https://github.com/pre-commit/mirrors-mypy
  #   rev: v1.19.1
  #   hooks:
  #     - id: mypy
  #       additional_dependencies: [torch, numpy, pandas-stubs]
  #       args: [--config-file=pyproject.toml]
  #       pass_filenames: false
